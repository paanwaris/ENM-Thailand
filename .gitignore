# R session
.Rhistory
.RData
.Rproj.user/
.Rapp.history
*.Rproj.user

# Per-user secrets — NEVER commit these
.Renviron
.env

# Knitr cache and figures from Rmd
*_cache/
*_files/

# Workshop data and outputs — too large for git, regenerated by Day2a_Data_Download.Rmd
data/raw/
data/processed/
outputs/*.tif
outputs/*.rds
outputs/*.RData
figures/*.png
figures/*.pdf

# pkgdown build output (built and pushed via GitHub Action / build script)
docs/

# OS junk
.DS_Store
Thumbs.db

# Renv (optional)
renv/library/
renv/local/
renv/cellar/
renv/lock/
renv/python/
renv/staging/

# R session
.Rhistory
.RData
.Rproj.user/
.Rapp.history
*.Rproj.user

# Per-user secrets — NEVER commit these
.Renviron
.env

# Knitr cache and figures from Rmd
*_cache/
*_files/

# Workshop data and outputs — too large for git.
# data/raw/ + data/processed/ are regenerated by Day2a_Data_Download.Rmd.
# temporal_rasters/ is hosted externally (Zenodo / GitHub Releases);
# fetch with scripts/download_temporal_rasters.R.
data/raw/
data/processed/
temporal_rasters/
outputs/*.tif
outputs/*.rds
outputs/*.RData
figures/*.png
figures/*.pdf

# pkgdown build output — uncomment if you publish via the gh-pages branch
# (with usethis::use_pkgdown_github_pages()) and don't want docs/ on main.
# docs/

# OS junk
.DS_Store
Thumbs.db

# Renv (optional)
renv/library/
renv/local/
renv/cellar/
renv/lock/
renv/python/
renv/staging/
docs
